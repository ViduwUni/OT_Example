import React from 'react';
import ReactDOM from 'react-dom/client';
import OTOnlyCalculator from './OTOnlyCalculator';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <OTOnlyCalculator />
  </React.StrictMode>
);